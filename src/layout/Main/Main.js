import styled from "@emotion/styled";

const Main = styled("section")(() => {
  return {
    display: "flex",
    justifyContent: "center",
    padding: "2em 4em",
    gap: "5em"
  };
});
export default Main;
